#pragma once

namespace Tmpl8
{
	/*class convexHull
	{
	public:
		convexHull(vector<vec2>& activeTanksPositions);
		void heapSort(vector<vec2>& activeTanksPositions, int sizeOfVector);
		void minHeapify(vector<vec2>& tanks, int index, int sizeOfVector, vec2 origin);
	private:
	};*/
}

